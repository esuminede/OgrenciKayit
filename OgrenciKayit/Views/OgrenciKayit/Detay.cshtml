@model OgrenciKayit.Models.Ogrenci

@{
    ViewBag.Title = Model.OgrenciNo;
}

@{
    ViewBag.Title = "Detay";
}

<h2>Detay</h2>

<div class="container md-4">
    <h2>Model.OgrenciNo - Model.Isim Model.Soyisim</h2>
    <hr />
    @if (Model.OgrenciNo > 0)
    {
        <p class="mt-3">
            @Html.Raw(Model.OgrenciNo)
        </p>
    }
    <a href="@Url.Action("Index", "Blog")" class="btn btn-secondary mt-3">Geri Dön</a>

</div>

